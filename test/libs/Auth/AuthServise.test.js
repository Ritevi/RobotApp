/* eslint-env mocha */
const { expect } = require('chai');
const { AuthService } = require('../../../libs/Auth');
const config = require('../../../config');

describe('AuthService test', () => {
  describe('register method', () => {
    it('register user with normal info', () => {

    });
  });
});
